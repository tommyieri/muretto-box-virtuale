# calendario_2026.json

Generato da `gen_calendario.py` da f1db (release CSV, tabella races 2026) + vincitori dai dati gara gia' pubblicati in `demo/data/`; la copia `demo/data/calendario_2026.json` e' l'unica letta dalla UI — non modificare a mano, rilanciare il generatore.
